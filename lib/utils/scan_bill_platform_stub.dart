import 'package:image_picker/image_picker.dart';

/// Web stub - ML Kit doesn't support web. Returns null to indicate OCR unavailable.
Future<String?> processImageForOcr(XFile xFile) async {
  return null;
}
